# 智能投放分析系统 - 技术规格说明书

## 1. 项目概述

### 项目名称
智能投放分析系统（Intelligent Investment Analysis System）

### 核心定位
面向自媒体投放从业者的自动化数据分析工具，输入关键词即可一键抓取微信公众号匹配账号，分析历史数据、预测未来数据、自动核算广告报价。

### 目标用户
- 自媒体投放从业者
- 广告优化师
- 品牌方市场人员

## 2. 技术架构

### 技术栈
| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | Streamlit | 轻量化、快速开发、数据工具专用 |
| 后端 | Python 3.9+ | 核心业务逻辑 |
| 爬虫 | requests + BeautifulSoup4 | 网页数据采集 |
| 数据处理 | pandas + numpy | 数据清洗、统计、预测 |
| 可视化 | Plotly | 交互式图表 |
| 导出 | openpyxl | Excel 文件导出 |
| 存储 | CSV/JSON | 本地文件存储 |

### 目录结构
```
/workspace/
├── SPEC.md
├── requirements.txt
├── app.py                    # Streamlit 主入口
├── config.py                 # 配置模块
├── models/
│   └── data_models.py        # 数据模型定义
├── services/
│   ├── crawler.py            # 爬虫服务
│   ├── data_processor.py     # 数据处理服务
│   ├── predictor.py          # 数据预测服务
│   └── pricing.py            # 报价计算服务
├── utils/
│   └── exporters.py          # 导出工具
└── data/                     # 数据存储目录
```

## 3. 功能模块详细设计

### 3.1 用户交互模块

#### 界面布局
- 侧边栏：参数配置
  - 关键词输入框
  - 单价系数滑块（0.5-1.0元/次，默认0.75）
  - 数据源选择（微信公众号/今日头条/全部）
  - 抓取数量限制
- 主区域：
  - 账号列表表格
  - 账号详情折叠面板
  - 可视化图表区域
  - 导出按钮组

#### 交互流程
1. 用户输入关键词
2. 点击"开始分析"按钮
3. 显示进度条和状态信息
4. 完成后展示账号列表
5. 支持排序、筛选、导出

### 3.2 文章搜索模块

#### 搜索逻辑
- 根据关键词匹配文章标题/内容
- 提取文章归属账号信息
- 支持多平台搜索

#### 模拟数据策略
当无真实接口时，使用随机生成的模拟数据确保演示可用。

### 3.3 数据爬虫模块

#### 采集数据字段
| 字段 | 类型 | 说明 |
|------|------|------|
| account_id | str | 账号唯一标识 |
| account_name | str | 账号名称 |
| account_field | str | 账号领域/分类 |
| article_title | str | 文章标题 |
| publish_time | datetime | 发布时间 |
| read_count | int | 阅读量 |
| like_count | int | 点赞量 |
| comment_count | int | 评论量 |
| recommend_count | int | 推荐量/在看量 |

#### 异常处理
- 网络请求超时：重试3次
- 数据解析失败：记录日志跳过
- 反爬限制：添加随机延时

### 3.4 数据处理模块

#### 基础统计
- 平均阅读量 = sum(阅读量) / count
- 平均点赞率 = sum(点赞量) / sum(阅读量)
- 平均评论率 = sum(评论量) / sum(阅读量)

#### 异常值剔除
- 爆文识别：阅读量 > 平均值 × 3
- 低质文识别：阅读量 < 平均值 × 0.1
- 剔除后重新计算平均值

#### 数据维度
- 近30天数据
- 近10篇头条文章数据

### 3.5 数据预测模块

#### 预测算法
```
下一篇文章预估阅读量 = 近10篇头条文章阅读量平均值（稳定权重）
```

#### 多维度预估
- 预估阅读量
- 预估点赞量
- 预估评论量
- 预估推荐量

### 3.6 报价计算模块

#### 核心公式
```
单篇头条文章报价 = 下一篇文章预估阅读量 × 单价系数
```

#### 报价区间
| 类型 | 计算方式 |
|------|----------|
| 最低报价 | 预估阅读量 × 0.5 |
| 标准报价 | 预估阅读量 × 0.75 |
| 最高报价 | 预估阅读量 × 1.0 |

#### 单价系数
- 默认值：0.75 元/次阅读
- 可调范围：0.5 - 1.0 元/次
- 用户可手动调整

## 4. 数据模型

### 4.1 Account（账号模型）
```python
{
    "account_id": str,
    "account_name": str,
    "account_field": str,
    "avg_read_count": float,
    "avg_like_rate": float,
    "avg_comment_rate": float,
    "article_count": int,
    "estimated_read": float,
    "price_min": float,
    "price_standard": float,
    "price_max": float
}
```

### 4.2 Article（文章模型）
```python
{
    "article_id": str,
    "account_id": str,
    "title": str,
    "publish_time": datetime,
    "read_count": int,
    "like_count": int,
    "comment_count": int,
    "recommend_count": int
}
```

## 5. 界面原型

### 5.1 侧边栏布局
```
┌─────────────────┐
│  ⚙️ 参数配置     │
├─────────────────┤
│ 关键词: [_____]  │
│ 单价系数: ──●──  │
│ 数据源: [▼ 全部] │
│ 抓取数量: [10]   │
│                 │
│ [🚀 开始分析]    │
└─────────────────┘
```

### 5.2 主界面布局
```
┌────────────────────────────────────────────────┐
│ 📊 智能投放分析系统              [导出Excel]    │
├────────────────────────────────────────────────┤
│ 账号名称  │ 领域  │ 预估阅读 │ 标准报价 │ 操作  │
│──────────┼───────┼──────────┼──────────┼───────│
│ 科技每日谈 │ 科技  │  125,800 │ ¥94,350  │ 查看  │
│ 财经观察  │ 财经  │   98,500 │ ¥73,875  │ 查看  │
│ ...       │ ...   │    ...   │ ...      │ ...   │
├────────────────────────────────────────────────┤
│ 📈 数据可视化                                   │
│   [账号对比柱状图]  [趋势折线图]               │
│                                                │
│   ████ ████ ████ ████ ████                    │
│                                                │
└────────────────────────────────────────────────┘
```

## 6. 验收标准

### 6.1 功能验收
- [ ] 关键词输入后能正确触发搜索
- [ ] 账号列表正确展示所有字段
- [ ] 数据排序功能正常（按预估阅读/报价）
- [ ] 报价计算公式正确执行
- [ ] Excel 导出功能正常

### 6.2 界面验收
- [ ] Streamlit 界面加载正常
- [ ] 侧边栏参数配置生效
- [ ] 表格数据展示完整
- [ ] 图表渲染正常

### 6.3 代码验收
- [ ] 无运行时错误
- [ ] 依赖库完整声明
- [ ] 模块化结构清晰
- [ ] 模拟数据可正常演示

## 7. 扩展性预留

### 接口预留
- 支持后续添加抖音、快手等平台
- 支持更多报价公式配置
- 支持数据库存储扩展

### 配置预留
- 报价系数可配置
- 数据源可切换
- 采集数量可调整
